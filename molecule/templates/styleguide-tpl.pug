extends ../../../../../node_modules/queo-styleguide/src/pug/atomic/template/modules
include ../../../app/atomic/molecule/<%= id %>

block headline
    | <%= category %>

block modules
    .container
        +<%= id %>()
        +highlight
          +<%= id %>()
