extends ../../../../../node_modules/styleguide/src/pug/atomic/template/modules

block headline
    | <%= category %>

block modules
    .container
        +<%= id %>()
        +highlight
          +<%= id %>()
